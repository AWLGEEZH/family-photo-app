# Production Environment Variables Template
# Copy this to .env.production and fill in your actual values
# NEVER commit the actual .env.production file to Git

# Server Configuration
NODE_ENV=production
PORT=8080

# Database Configuration - MongoDB Atlas
# Get this from MongoDB Atlas: Clusters → Connect → Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/family-photo-app?retryWrites=true&w=majority

# Security - JWT Secret
# Generate a secure random string (32+ characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-at-least-32-characters-long

# Cloudinary Configuration - Media Storage
# Get these from Cloudinary Dashboard
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Frontend URL for CORS
# Update this with your actual Vercel deployment URL
CLIENT_URL=https://your-app-name.vercel.app